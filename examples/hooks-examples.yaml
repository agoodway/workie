# Workie Hook Examples
# ===================
# This file demonstrates various hook usage patterns for different development workflows

files_to_copy:
  - .env.example
  - package.json
  - docker-compose.yml

hooks:
  # Set timeout for all hooks (default: 5 minutes)
  timeout_minutes: 10

  # Post-creation hooks - run after creating a new worktree
  post_create:
    # Basic setup commands
    - "echo '🚀 Setting up new development environment...'"
    
    # Install dependencies based on project type
    - "npm install || echo '⚠️ npm install failed or not needed'"
    - "pip install -r requirements.txt || echo '⚠️ pip install failed or not needed'"
    - "bundle install || echo '⚠️ bundle install failed or not needed'"
    
    # Setup development database
    - "docker-compose up -d db || echo '⚠️ Database setup failed or not needed'"
    
    # Run database migrations
    - "npm run migrate || echo '⚠️ Migrations failed or not needed'"
    
    # Setup git hooks
    - "cp .githooks/* .git/hooks/ || echo '⚠️ Git hooks setup failed or not needed'"
    - "chmod +x .git/hooks/* || echo '⚠️ Git hooks permissions failed or not needed'"
    
    # Create necessary directories
    - "mkdir -p logs tmp cache || echo '⚠️ Directory creation failed'"
    
    # Setup completion message
    - "echo '✅ Environment setup complete! Happy coding!'"

  # Pre-removal hooks - run before removing a worktree
  pre_remove:
    # Clean up processes
    - "echo '🧹 Cleaning up development environment...'"
    
    # Stop running services
    - "docker-compose down || echo '⚠️ Docker cleanup failed or not needed'"
    
    # Clean up temporary files
    - "rm -rf logs/* tmp/* cache/* || echo '⚠️ Temp cleanup failed'"
    
    # Archive important data
    - "tar -czf ../backup-$(date +%Y%m%d-%H%M%S).tar.gz . || echo '⚠️ Backup failed'"
    
    # Final cleanup message
    - "echo '✅ Cleanup complete!'"

# Security Note:
# - Always use '|| echo' patterns to handle command failures gracefully
# - Avoid destructive commands like 'rm -rf /' 
# - Test hooks thoroughly before using in production
# - Use relative paths and validate inputs
# - Be cautious with network commands and file permissions
